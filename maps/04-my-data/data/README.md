# Day 4 Data Outputs

Sanitized datasets generated by the Day 4 processing scripts live here (for example `disney-park-visits.geojson`). The raw personal exports stay in `private-data/` at the repository root and are gitignored.
