<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ pageTitle }}</title>
    <link rel="icon" type="image/svg+xml" href="{{ faviconPath | default(basePath + 'favicon.svg') }}">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    {% block styles %}
    <style>
        {% include "partials/base-styles.njk" %}
        {% block page_styles %}{% endblock %}
    </style>
    {% endblock %}
</head>
<body class="{{ bodyClass | default('') }}">
    {% block body %}{% endblock %}

    <script src="{{ basePath }}site-assets/js/day-navigation.js" data-day="{{ daySlug }}" data-target="[data-day-nav-top]" data-manifest="{{ manifestPath }}" data-base-path="{{ basePath }}" defer></script>
    <script src="{{ basePath }}site-assets/js/day-navigation.js" data-day="{{ daySlug }}" data-target="[data-day-nav]" data-manifest="{{ manifestPath }}" data-base-path="{{ basePath }}" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
